import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginVertical: 16,
    marginRight: 16,
    marginBottom: 24,
    alignSelf: 'flex-start',
  },
});

import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 16,
    marginBottom: 24,
  },
  text: {
    position: 'absolute',
    bottom: 15,
    left: 20,
  },
});
